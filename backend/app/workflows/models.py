"""SQLAlchemy models for workflow orchestration.

Orchestration: WorkflowConfig records and the Run lifecycle (Run, RunFile).
Models define tables against `app.core.db.Base`. Empty scaffold — no schema yet.
"""
