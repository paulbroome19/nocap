"""SQLAlchemy models for the comparison stage.

v2 scaffold only: our zip vs vendor zip fact-level regression diff.

Models define tables against `app.core.db.Base`. Empty scaffold — no schema yet.
"""
