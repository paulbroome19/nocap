"""HTTP routes for the comparison stage — thin: parse, call service, shape response.

Empty scaffold: an `APIRouter` with no routes yet, wired in `app.main`.
"""

from fastapi import APIRouter

router = APIRouter()
