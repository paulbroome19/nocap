import PagePlaceholder from '../components/PagePlaceholder'

export default function Snapshots() {
  return (
    <PagePlaceholder
      title="Snapshots"
      description="Uploaded EBA DPM taxonomy releases — immutable, versioned snapshots."
    />
  )
}
