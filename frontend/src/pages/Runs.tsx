import PagePlaceholder from '../components/PagePlaceholder'

export default function Runs() {
  return (
    <PagePlaceholder
      title="Runs"
      description="Submission runs — inputs, validation findings, and output packages."
    />
  )
}
