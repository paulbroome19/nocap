import PagePlaceholder from '../components/PagePlaceholder'

export default function Workflows() {
  return (
    <PagePlaceholder
      title="Workflows"
      description="Configured reporting suites (v1: COREP LCR) and their run history."
    />
  )
}
